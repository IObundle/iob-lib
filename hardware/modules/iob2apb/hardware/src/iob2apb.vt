   iob2apb
     #(
       .APB_ADDR_W(APB_ADDR_W),
       .APB_DATA_W(APB_DATA_W)
       )
   iob2apb0
     (
      // APB master interface
`include "<core name prefix>apb_portmap.vs"

      // IOb slave interface
`include "<core name prefix>portmap.vs"

      // Global signals
`include "iob_clk_en_rst_portmap.vs"
      );
