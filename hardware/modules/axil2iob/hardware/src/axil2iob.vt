   axil2iob
     #(
       .AXIL_ADDR_W(AXIL_ADDR_W),
       .AXIL_DATA_W(AXIL_DATA_W),
       .AXIL_ID_W(1)
       )
   axil2iob0
     (
      // AXI4 Lite slave interface
`include "<core name prefix>axil_portmap.vs"

      // IOb master interface
`include "<core name prefix>portmap.vs"

      // Global signals
`include "iob_clkrst_portmap.vs"
      );
