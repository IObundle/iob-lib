   iob2apb
     #(
       .APB_ADDR_W(APB_ADDR_W),
       .APB_DATA_W(APB_DATA_W)
       )
   iob2apb0
     (
      // APB master interface
`include "<core name prefix>apb_portmap.vh"

      // IOb slave interface
`include "<core name prefix>portmap.vh"

      .clk(clk_i),
      .rst(rst_i)
      );
